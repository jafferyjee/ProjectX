@using Kendo.Mvc.UI

@(Html.Kendo().DropDownListFor(m => m)
    //.Filter(FilterType.StartsWith)
    //.AutoBind(false)
    .DataTextField("SubInvName")
    .DataValueField("SubInvCode")
    .BindTo(ProjectX.Web.Utility.Lookups.GetSubInventory())
    //.DataSource(datasource => datasource
    //    .Custom()
    //    .Type("odata")
    //    .Transport(transport =>
    //        {
    //            transport.Read(read => read.Url("/odata/SubInventory?$select=SubInvCode,SubInvName").DataType("json"));
    //        }
    //    )
    //    .Schema(schema => schema
    //        .Data("value")
    //        .Total("count")
    //    )
    //)
)
@Html.ValidationMessageFor(m => m)
